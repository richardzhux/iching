import type { MetaphysicsStatistics } from "@/types/api"

export function ComparisonUniverseBar({ statistics, locale }: { statistics: MetaphysicsStatistics; locale: "en" | "zh" }) {
  const baseline = statistics.baseline
  return <details className="rounded-xl border border-border/55 bg-background/55 px-4 py-3"><summary className="cursor-pointer text-xs font-semibold text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary">{locale === "zh" ? "对照范围" : "Comparison universe"}<span className="ml-2 font-normal text-muted-foreground">{baseline.start}–{baseline.end} · {baseline.unique_state_count?.toLocaleString() ?? "—"} {locale === "zh" ? "个唯一状态" : "unique states"} · {baseline.sample_unit}</span></summary><div className="mt-3 border-t border-border/50 pt-3 text-xs leading-5 text-muted-foreground"><p>{locale === "zh" ? "配置" : "Configuration"}: {baseline.config_id ?? baseline.id}</p><p>{locale === "zh" ? "引擎" : "Engine"}: {baseline.engine}</p><p>{locale === "zh" ? "方法" : "Method"}: {baseline.method}</p><p>{locale === "zh" ? "样本权重" : "Sample weight"}: {baseline.sample_weight.toLocaleString()}</p><p>{locale === "zh" ? "规则版本" : "Rule version"}: {baseline.rules_version}</p><p>{locale === "zh" ? "百分比表示该结构在本历法对照样本中的加权出现率。" : "The percentage is the weighted incidence of this structure in the calendar reference sample."}</p></div></details>
}
